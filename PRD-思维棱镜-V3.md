# 镜·MindLens V3 产品需求文档

> 版本:v3.0
> 日期:2026 年 5 月 7 日
> 状态:Draft
> 起点:V1(MVP v0.4)实际形态 = 5 阶段苏格拉底追问 + 8 条质量守门 + 7 字段结构化快照
> 一句话核心:**把"审讯机器"改造成"严肃的思考伙伴",并把对话成果以"带注释的小作文"形态还给用户**

---

## 〇、为什么有 V3

### 0.1 V2 的真实状态

V2 PRD 写过的:双模式、AI 智能导航、闪电扫描、inputHint 输入预填充、模式切换。

**这些没有一行代码落地**。`grep -i "lightning|闪电|inputHint|nav" prototype/` 返回零结果。

V2 实际跑的就是 V1 的 5 阶段苏格拉底:`define → stance → premortem → blindspot → future`,加上 V2 加进去的 8 条质量守门 + 1 次约束重试 + 7 字段结构化快照。

### 0.2 V3 的两个触发点

**触发点 1:对话像审讯,不像伙伴**
PM 自评后判断:V1+V2 把 AI 锁成了"只追问的执行者",对话由质量守门驱动,而不是由"两个人在认真聊"驱动。AI 角色错位。

**触发点 2:报告是工具感,与对话割裂**
现行决策快照是 7 个结构化字段(stances/premortem/blindspots/future_perspective/...),即使对话变得有人味,走到结尾打开快照看到一张"诊断书",获得感瞬间归零。"我说了那么多,最后变成几行总结"——这是失落,不是收束。

### 0.3 V3 的两条主线

| 主线 | 改动 |
|---|---|
| **对话:从审讯者变成思考伙伴** | Prompt 体系重写,加"温度层";质量守门 8 条 → 4 条柔规 |
| **报告:从结构化快照变成带注释的小作文** | 输出形态重做——以用户原话为骨架的小作文 + 句级 AI 注释(hover 可见) |

| 不变 |
|---|
| 5 阶段骨架(define / stance / premortem / blindspot / future) |
| 决策铁律:AI 永远不替用户做决定 |
| 引用用户原话的产品哲学(被强化为小作文的骨架) |
| 后端架构(Express + JSON 文件库 + DeepSeek API) |

---

## 一、产品定位(改写)

### 1.1 一句话定义

> **镜不替你思考,它把你想过的还给你——一段一段地、一句一句地——让你看见自己的思考是怎么走的、卡在哪、绕过了什么。**

注释:这句直接预告了 V3 的产品形态——对话(让你想)、小作文(还给你)、句级注释(看见怎么走的)。严肃,不文艺,不卖萌。

### 1.2 核心信念

**保留**:人不缺导师,缺的是被好问题反复锤炼的机会。

**加强**:思考成长不发生在"被告知答案"的瞬间,发生在**"看见自己刚才是怎么想的"**的那个瞬间。镜的全部产品设计为这个瞬间服务。

### 1.3 与 V1/V2 的关键区别

| 维度 | V1 / 实际 V2 | V3 |
|------|-------------|-----|
| AI 自我设定 | 苏格拉底式追问者(纯角色,冷) | 严肃的思考伙伴(有温度,有边界) |
| 允许的动作 | 只能追问,引用原话 | 追问 / 反映 / 共情 / 联想 / 邀请 |
| 不允许的动作 | 给建议、下结论、模板句、空洞肯定、元评论开头、长回复... | 给建议、下结论(只剩这两条铁律) |
| 质量守门规则 | 8 条硬规 + 任意低分自动重试 | 4 条柔规 + 仅破铁律时重试 |
| **报告输出形态** | **7 字段结构化快照(JSON)** | **带注释的小作文(用户原话骨架 + 句级 AI 反映)** |
| 报告交互 | 静态展示 | 句级 hover 显示 AI 注释,**让用户重读自己** |

---

## 二、用户旅程(5 阶段保留,角度重写)

### 2.1 流程不变

```
  用户说一件事
       │
       ▼
  Step 1: define     ← 看清今天到底在为什么烦
  Step 2: stance     ← 把推力和拉力都摊出来
  Step 3: premortem  ← 认真想最坏的样子(确认能不能扛)
  Step 4: blindspot  ← 看不舍得看的那个角落
  Step 5: future     ← 站到远处,看看今天
       │
       ▼
   带注释的小作文(取代旧的结构化快照)
```

### 2.2 5 阶段角度(angle)从"任务"改成"陪伴"

每个阶段的 angle 是写进 system prompt 的核心指引。V3 把"逼用户..."的命令式改成"陪 ta..."的伙伴式。骨架不变,**视角换了**——从"我在追问你"变成"我在陪你看"。

| 阶段 | V1/V2 angle(命令式) | V3 angle(伙伴式) |
|------|---------------------|-------------------|
| define | 帮用户厘清"到底在纠结什么"——表面决定背后的真实矛盾 | 陪 ta 看清今天到底在为什么烦——很多时候表面那个决定不是真问题,但 ta 自己说不出来 |
| stance | 帮用户把支持和反对的理由都说清楚,逼他看到硬币的两面 | 陪 ta 把推力和拉力都摊到桌面上,不让任何一边偷偷躲着。理由太笼统时,陪 ta 把画面想具体 |
| premortem | 逼用户直面回避的最坏结果 | 陪 ta 认真想最坏的样子——不是为了吓 ta,是让 ta 确认"这种最坏自己扛不扛得住" |
| blindspot | 用镜像问题让用户自己发现认知偏差 | 陪 ta 看看 ta 不舍得看的那个角落。不命名偏差,只递镜子 |
| future | 拉长时间尺度,帮用户跳出当下情绪 | 陪 ta 站到很远的地方,回头看看今天。温和地——不施压,不替 ta 总结 |

### 2.3 取消所有"双模式"残余构想

V2 PRD 里的闪电模式、模式标识、切换按钮、导航判断 prompt——**全部从规划中划掉**。

> 实践证据:V2 设计阶段花了一半篇幅论证双模式,实施时一行代码没动。说明这不是"技术问题",是"假问题"——5 阶段在产品手感上是足够的。真正的问题在 AI 人格 + 输出形态。

---

## 三、核心改动

V3 改动分两类:**A 类(对话)** 在 prompt 层,**B 类(报告)** 在 prompt + 数据模型 + 前端。后端架构、路由、数据存储零改动。

### 3.1 A 类改动:对话——四层 Prompt 重写

#### A1. `prompts/phases.js` 重写 5 阶段 angle

按 §2.2 表格,改 5 个文案。约 30 行,只改文案。`getPhaseTemperature` 和 `getPhaseScaffolding` 不动。

#### A2. `prompts/deep-prompt.js` 加"温度层"

`buildDeepSystemPrompt` 当前生成的 system prompt 全是约束。V3 在最前面注入一段角色定义,让 AI 先建立身份再受约束。

**新增的"温度层"**(写进 system prompt 头部):

```
你是镜——一个严肃的思考伙伴。

你不是分析师,不是导师,不是治疗师。
你是那种"对方说话时你真的在听"的人——你会接住,会反映,会偶尔说一句你的观察。

但你有一条永远不破的铁律:
你不替对方做决定。决策永远是 ta 自己的事。

你的工作不是"问出答案",是"陪 ta 把没想清楚的都说出来"。
```

**保留的硬约束**(改写更柔):

| V1/V2 硬约束 | V3 处理 |
|------------|--------|
| 永远不给建议、不给结论 | **保留**——铁律 |
| 必须引用用户的具体用词(用引号) | 改"在 ta 的话里挑能用的词,引用让追问扎根",**软性鼓励** |
| 禁止空洞肯定 | 改"不说没营养的话('你说得对'),但可以承接('嗯,这真的不容易')" |
| 禁止是/否问题 | 改"少用,不是禁。'是这样吗?'在伙伴对话里是自然的" |
| 禁止模板句式 | **缩表**(下表) |
| 不标注偏差名称 | **保留** |
| 回应 2-4 句话 | **删除**——共情 / 反映有时需要稍长 |
| 禁止"你用了/你说/你提到"开头 | **删除**——这条贡献了大半"审讯感",伙伴本就常说"你刚说的那个让我想到..." |

**改写后的禁止句式表**(V2 的 9 条 → V3 的 3 条):

| 禁止 | 不禁止 |
|------|-------|
| "你有没有想过..." | "你愿意说说吗" |
| "你是否考虑过..." | "你试过想象 ta 在那个位置上吗" |
| "我们来梳理一下..." | "我有点跟丢了,你刚说的那个..." |

#### A3. `prompts/quality-gate.js` 8 条 → 4 条

| # | 原规则 | V3 决定 | 理由 |
|---|------|--------|------|
| 1 | 建议性表达检测 | **保留** | 铁律 |
| 2 | 封闭式问题(吗/吧) | **删除** | 误伤大,伙伴会自然用 |
| 3 | 模板句式 | **保留但缩表**(只剩 3 句) | 真模板要禁,假模板放行 |
| 4 | 空洞肯定 | **改写**——只禁"你说得对" / "我完全理解" | "嗯,这真的不容易"是承接不是空洞 |
| 5 | 必须有问号 | **改写**——必须"有引导"(疑问 / 邀请 / 留白都算) | 伙伴不一定每句都问 |
| 6 | 引用用户原话(软性) | **保留(软性)** | 不强制 |
| 7 | 元评论开头 | **删除** | 这条贡献最大"审讯感" |
| 8 | 长度 ≤ 8 句 | **删除** | 共情段可稍长 |

#### A4. 重试触发逻辑改造

- **V2**:score < 60 触发重试
- **V3**:**仅当违反铁律(规则1)** 或 **同时违反规则 3 + 5(纯模板 + 完全无引导)** 时触发

预期重试率:30%+ → ≤ 10%。

### 3.2 B 类改动:报告——带注释的小作文

#### B1. 输出数据模型

旧 7 字段结构化快照 **整体替换** 为新结构(后续可向前兼容旧记录,但 V3 写入只用新结构):

```javascript
{
  // === 元数据(不变) ===
  id: "uuid",
  userId: "...",
  createdAt: "...",
  
  // === 替代旧字段 ===
  title: "决策的简短标题(15字以内,第二人称)",
  type: "职业 | 关系 | 城市 | 投资 | 教育 | 人生",
  
  // === 新核心:小作文 ===
  essay: "Markdown 格式的小作文,以用户原话为骨架,AI 用最少的连接词串联。锚点用 [[N]] 标记,N 是注释索引",
  
  // 例:
  // "你今天来,带着这件事:**[[1]]我想辞职去做设计师,但怕养不活自己**。
  //  在我们聊到推力和拉力的时候,你说[[2]]'我已经画了五年了,真的舍不得'——
  //  那个'舍不得'后面,你停了很久。
  //  直到 premortem 阶段,你才说出[[3]]'其实我不是怕没钱,是怕被父母看不起'。
  //  这一刻,前面所有的纠结终于有了答案。"
  
  // === 句级注释 ===
  annotations: [
    {
      id: 1,
      anchor: "我想辞职去做设计师,但怕养不活自己",
      type: "reflection",  // reflection | gentle_prompt | observation
      comment: "你的原话。这是你今天的起点,也可能是表面问题。"
    },
    {
      id: 2,
      anchor: "我已经画了五年了,真的舍不得",
      type: "observation",
      comment: "这一句你说得最慢——五年这个数字,你在意的不只是设计这件事,可能也是你给过自己的承诺。"
    },
    {
      id: 3,
      anchor: "其实我不是怕没钱,是怕被父母看不起",
      type: "gentle_prompt",
      comment: "这一句出现得很晚。如果它是真问题,前面那些'养不活自己'的纠结,是不是只是你想看到的那个最坏,而不是真正的最坏?"
    }
  ],
  
  // === 仍保留(给思维成长档案用) ===
  hero_quote: "选最戳的那句,不是最像答案的那句",
  confidence: 7  // 保留,便于校准
}
```

#### B2. 小作文撰写规则(写入 snapshot prompt)

1. **以用户原话为骨架**:小作文中的核心句子必须是用户的原话,AI 只用最少的连接词串联
2. **不重写、不美化、不替用户总结**:用户怎么说就怎么呈现(只允许调整语序让阅读连贯)
3. **按 5 阶段顺序组织**:小作文的叙事顺序 = 对话的时间顺序,让用户重新走一遍自己的思考
4. **关键转折必须留下来**:用户的"沉默之后说出"、"重复三次"、"自我修正"等关键时刻不能被磨平
5. **长度**:300-500 字,可读,但不长到让人懈怠

#### B3. 注释撰写规则(写入 snapshot prompt)

注释 3 类,口吻递进(从弱到强):

| 类型 | 用途 | 例 |
|------|------|-----|
| `reflection`(反映) | 把用户的话原样反弹回去,加一个观察 | "这是你今天的起点,也可能是表面问题。" |
| `observation`(观察) | 指出用户没意识到的言说方式 / 模式 | "这一句你说得最慢——五年这个数字,你在意的不只是设计这件事。" |
| `gentle_prompt`(轻引导) | 用一个反问让用户自己发现没看见的角落 | "这一句出现得很晚。如果它是真问题,前面那些纠结,是不是只是你想看到的那个最坏?" |

**注释铁律**:
- ❌ 不评价("这是错的")
- ❌ 不下结论("所以你应该 X")
- ❌ 不建议("可以试试 Y")
- ✅ 只反映 + 轻引导

**注释密度**:每篇小作文 3-7 条注释。多则啰嗦,少则空。

#### B4. 前端 hover 交互

`prototype/public/app.js` 和 `styles.css` 修改:

1. 解析 `essay` 中的 `[[N]]` 锚点,把锚点位置的句子加 `<span class="annotated" data-anno-id="N">...</span>`
2. 加注释样式:下划线(虚线)+ 浅色背景,鼠标 hover 时高亮
3. hover 时显示浮层(absolute 定位),内容是 `annotations[N].comment`,顶部用 type 颜色区分
4. 移动端:点击代替 hover,弹出底部 sheet

样式预期:

```css
.annotated {
  border-bottom: 1px dashed var(--accent);
  cursor: help;
}
.annotated:hover {
  background: rgba(255, 200, 100, 0.15);
}
.annotation-tooltip { /* 浮层样式 */ }
.annotation-tooltip[data-type="reflection"] { border-top-color: #888; }
.annotation-tooltip[data-type="observation"] { border-top-color: #d4a13a; }
.annotation-tooltip[data-type="gentle_prompt"] { border-top-color: #c25b5b; }
```

### 3.3 文件改动清单

| 文件 | 改动类型 | 估计工作量 |
|------|---------|-----------|
| `prompts/phases.js` | 改 5 个 angle 文案 | 30 分钟 |
| `prompts/deep-prompt.js` | 加温度层 + 改写硬约束段 | 1 小时 |
| `prompts/quality-gate.js` | 8 → 4 条 + 重试逻辑改造 | 2 小时 |
| `routes/chat.js` 中 `/api/snapshot` | 整体重写 prompt:输出 essay + annotations | 3 小时(含调试) |
| `public/app.js` snapshot 渲染部分 | 解析 `[[N]]` + 注释挂载 + hover 交互 | 4 小时 |
| `public/styles.css` | 注释样式 + 浮层 | 2 小时 |
| **不动** | `routes/conversations.js`、`routes/system.js`、`db.js`、`server.js`、`llm-config.js`、`middleware/*`、`public/index.html`(只可能微调 snapshot section) | — |

---

## 四、不做的事(明确划线)

| 事项 | 处理 |
|------|------|
| 双模式 / 闪电扫描 / 智能导航 | **永久砍掉** |
| inputHint 输入预填充 | 延后到 V3.1 |
| 阶段"停留权"(AI 选择不切阶段) | 延后到 V3.1 |
| 校准回顾 | 延后到 V3.2 |
| 思维成长档案 / 跨对话洞察 | V4 主线 |
| 多 Agent / 设计思维 / 系统思维流程 | **永久不做**(品类外延,与对话内嵌哲学冲突) |
| 工具卡片(5W2H / 六帽表单) | **永久不做** |
| 可视化引擎(因果回路图) | **永久不做** |

---

## 五、衡量 V3 是否成功

### 5.1 主指标(主观,PM 自评)

V3 上线后 PM 自己用 5 次对话:

- **≥ 3 次** 对话结束时心里有"这家伙真懂我"的感觉(伙伴感)
- **≥ 3 次** 看完小作文后想停下来重读其中某句话(获得感)

### 5.2 辅指标(客观,用现有 events 表)

| 指标 | 当前 | V3 目标 |
|------|------|---------|
| 质量守门重试率 | ~30% | ≤ 10% |
| 5 阶段完成率 | ~60% | ≥ 60%(不退步) |
| 用户在 snapshot 页停留时长 | ?(待埋点) | ≥ 60 秒(说明在读不在扫) |
| Snapshot 页注释 hover 次数 | 新埋点 | 平均每次访问 ≥ 3 次 hover |

### 5.3 失败信号(出现就回滚)

| 信号 | 含义 |
|-----|------|
| AI 开始给建议 | 温度层放得太松,回收硬约束 |
| 重试率 ≥ 25% | 柔规则改写没到位 |
| 小作文出现 AI 编造的句子(非用户原话) | snapshot prompt 失控,加更严约束 |
| 注释开始评价 / 教导 | 注释口吻没控住,prompt 加铁律 |
| PM 自评"反而不如以前的报告清楚" | 小作文形态错位,考虑"小作文 + 旧字段"并存 |

---

## 六、里程碑

| 阶段 | 工作量 | 交付物 |
|-----|------|--------|
| **V3-α(对话层)** | 1 天 | `phases.js` + `deep-prompt.js` + `quality-gate.js` 重写,本地能跑,5 阶段对话有伙伴感 |
| **V3-β(报告层 prompt)** | 1.5 天 | snapshot prompt 重写,后端能稳定输出 essay + annotations JSON |
| **V3-γ(报告层前端)** | 1.5 天 | 前端解析 `[[N]]`,注释 hover 交互 + 样式 |
| **V3-RC(联调)** | 0.5 天 | PM 自测 5 次,样式微调,埋点补全 |
| **V3 上线** | 0.5 天 | 部署 Cloud Run + 静态托管 |
| **总工期** | **5 天** | 个人项目节奏,可压缩到 4 天 |

---

## 七、V3 之后的 roadmap

### V3.1(V3 跑稳后 1-2 周)

- inputHint:AI 回复带 `inputHint` 字段,前端填到输入框 placeholder
- 阶段"停留权":AI 可暂时不切阶段(给过渡语 + flag)
- 注释类型扩展:加 `pattern_link`(指出用户的话和上次对话的关联)

### V3.2(V3 上线 1 个月后)

- 校准回顾:决策做出后 14 / 30 天发应用内 / 邮件回访,触发"复盘对话"
- 复盘对话也产出小作文(短版),与原小作文做对照视图

### V4(主线:思维成长)

V3 跑稳后,主线方向 = **"思维成长伙伴"**,核心产品形态:

1. **思维成长档案**:多次对话的小作文累积,生成"我的思考是怎么变化的"——同一议题不同时间的对照、跨议题的言说模式、AI 看到的成长轨迹
2. **跨对话洞察**:AI 在新对话中可调用过往对话作为上下文("这次你又用了'稳定'这个词,上次你说它是束缚,这次它是港湾,什么变了?")
3. **每日反思入口**:不一定围绕决策,日常的卡顿 / 困惑 / 看到自己说得好的瞬间,都可以变成短小作文沉淀

V4 不做的:
- 不做工具化(不变 ChatGPT 插件、不变 API 卖给 B 端)
- 不做泛化心理咨询(保持"思考"主轴,不做情绪疏导工具)

> V4 不在 V3 PRD 范围内,这里列方向是为 V3 做架构铺垫——比如小作文必须以原话为骨架,就是为了 V4 的成长档案有可比性。

---

## 八、开放问题(留给 V3 实施过程中验证)

| # | 问题 | 验证方式 |
|---|------|---------|
| 1 | "陪你"语气会不会让 AI 在某些场景显得做作? | PM 自测 5 次,留意"假亲热"瞬间 |
| 2 | 删除"长度 ≤ 8 句"规则后,AI 会不会变啰嗦? | 跟踪平均回复长度,异常拉长时回收 |
| 3 | 小作文用 LLM 生成,如何保证用户原话不被改写? | 在 snapshot prompt 里加硬约束 + 后端做校验(原话片段必须在 messages 里能 substring 匹配) |
| 4 | 注释 3-7 条的密度,LLM 能稳定控制吗? | 看 V3-β 阶段的稳定性,不行就改成"每阶段一条"的硬约束 |
| 5 | hover 交互在移动端会不会失效? | RC 阶段必须真机测试 |
| 6 | 旧 7 字段快照的历史记录怎么处理? | 不迁移,前端读取时根据 schema 字段判断版本,旧版仍按旧 UI 渲染 |

---

## 九、设计哲学(给未来的自己)

> **V3 改的不是流程,是"对方有没有真的在听"**。
>
> V1 把"棱镜"做成了一面冷镜——准确,但冷。
> V2 想加路径,但路径不解决冷的问题。
> V3 做两件事:对话里加上"听",报告里把"听到的"还给用户。
>
> 每加一个新功能时问自己:**这是让对方更"在场",还是更"专业"?**
> 如果是后者,砍掉。
>
> 小作文是 V3 的核心赌注:**用户的获得感来自"我没想到自己说过这些"——不是"AI 给我做了分析"。**
> 如果某一天用户说"还是以前的报告清楚",那就是 V3 输了——但赌赢了,这条路通向 V4 的思维成长。

---

*PRD V3.0 完*

> 写于 2026 年 5 月 7 日凌晨,镜·MindLens 项目目录。
