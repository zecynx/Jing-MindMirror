# 镜·MindLens V2 产品需求文档

> 版本：v2.0  
> 日期：2026年4月17日  
> 状态：Draft  
> 基于：思维棱镜 MVP v0.4 原型改造  
> 核心理念：**本系统是帮助用户进行思考的外层骨骼框架**

---

## 一、产品定位

### 1.1 一句话定义

**镜是一款不替你思考、但让你无法偷懒的AI思维外骨骼——你只管说话，它帮你把没想清楚的都逼出来。**

### 1.2 核心信念

> 人不缺导师，缺的是被好问题反复锤炼的机会。  
> 镜不是"帮你想"，而是**给你一个无法偷懒的思考结构**——你自己的话，被你自己的逻辑，逼到无处可躲。

### 1.3 与 V1 的核心区别

| 维度 | V1（思维棱镜 MVP） | V2（镜·MindLens） |
|------|-------------------|-------------------|
| **路径** | 只有一种苏格拉底深度追问 | AI 自动识别问题类型，导航至深度/闪电两条路径 |
| **结构化引导** | 无（纯自由对话） | 对话内嵌引导——AI 追问自带结构化方向，输入框预填充模板提示 |
| **工具卡片** | 无 | 不做独立卡片，结构化信息通过 AI 追问 + 输入框预填充实现 |
| **用户感知** | 不知道自己在哪种思维流程中 | AI 明确告知当前思维模式，用户始终有方向感 |
| **流程弹性** | 5 阶段固定推进 | 深度模式 5 阶段 + 闪电模式 3 步快扫，AI 动态调整节奏 |

---

## 二、用户旅程：两种思考模式

用户打开镜，只做一件事：**说出你在纠结什么**。  
AI 判断问题类型，自动导航至对应的思维流程，并告知用户。

```
用户："我该不该辞职去创业？"
    │
    ▼
┌─────────────────────────────────────────────┐
│  AI 判断 + 导航（第一层：流程导航层）         │
│                                              │
│  "这听起来是一个重大决定。                    │
│   我会用「深度追问」帮你把每个角度都想清楚。  │
│   准备好了吗？"                               │
└─────────────────────────────────────────────┘
```

```
用户："今天两个会冲突了，该去哪个？"
    │
    ▼
┌─────────────────────────────────────────────┐
│  AI 判断 + 导航                               │
│                                              │
│  "日常选择，不需要纠结太久。                  │
│   我用「闪电扫描」帮你3分钟理清。             │
│   开始？"                                     │
└─────────────────────────────────────────────┘
```

### 2.1 深度追问模式

**适用场景**：重大决策、人生转折、反复纠结的问题  
**典型用户输入信号**：
- "该不该辞职/分手/换城市/创业/读研"
- "纠结了很久"
- "不知道怎么选"
- 涉及长期影响、不可逆、核心价值冲突

**流程设计**（5 阶段，复用现有引擎）：

```
Step 1：定义决策
│  AI："你现在正在纠结什么决定？"
│  → AI 追问找到表面决定背后的真实矛盾
│
Step 2：立场澄清
│  AI 逐层深挖支持和反对的理由
│  → 结构化引导：AI 追问自带方向
│     例："你说想换工作是因为'无聊'——
│          这个'无聊'是工作内容重复，
│          还是觉得这些工作没有意义？"
│
Step 3：Pre-Mortem
│  AI 逼用户想象两种选择各自由的最坏结果
│  → 结构化引导：AI 用条件句引导具体化
│     例："假设3年后你选择了改变，结果是一场灾难。
│          最可能的原因是什么？不是因为运气，
│          而是因为你做决定时忽视了什么？"
│
Step 4：盲区探测
│  AI 识别认知偏差，用镜像问题揭示
│  → 结构化引导：基于用户原话构造反直觉问题
│     例："你说'已经5年了'和'舍不得'——
│          如果这5年的投入都清零了，
│          你还会选同样的路吗？"
│
Step 5：未来回望
│  AI 帮用户从10年后回看当下
│  → 温暖收束，不施加压力
│
▼
决策快照 + 校准提醒
```

**与 V1 的区别**：
- 阶段推进时，AI 会在追问中嵌入结构化方向（5W2H式拆解、二阶思维引导），但**不以卡片形式出现**，而是融合在对话自然流中
- AI 在阶段切换时明确告知用户进入了哪个阶段

### 2.2 闪电扫描模式

**适用场景**：日常小决策、时间有限、不是核心纠结  
**典型用户输入信号**：
- "两个会冲突了该去哪个"
- "要不要买这个/吃这个"
- "周末去哪"
- 日常选择、低风险、短期影响

**流程设计**（3 步快扫）：

```
Step 1：快速定性
│  AI："这件事对你来说，什么最重要？"
│  → 用一个追问锁定决策核心维度
│
Step 2：两面翻转
│  AI："选A的话，最让你不舒服的是什么？
│       选B的话，最让你期待的是什么？"
│  → 快速翻面，让用户看到硬币的另一面
│     （灵感来自六帽精简版：黄帽-价值 / 黑帽-代价）
│
Step 3：一锤定音
│  AI："如果现在必须选，你的直觉是什么？"
│  → 帮用户确认自己的直觉倾向
│     追问：3个月后你会因为没选另一个而后悔吗？
│
▼
简短思考摘要（非完整快照）
```

**与深度模式的关键差异**：

| 维度 | 深度追问 | 闪电扫描 |
|------|---------|---------|
| 阶段数 | 5 阶段 | 3 步 |
| 对话轮数 | 8-15 轮 | 3-5 轮 |
| 输出 | 完整决策快照 + 校准 | 简短思考摘要 |
| 节奏 | 有松有紧，像呼吸 | 快节奏，不拖泥带水 |
| AI 语气 | 温和→加压→锐利→温暖 | 直接、轻快、像一个聪明的朋友 |
| 信心指数 | 需要（1-10） | 不需要 |

---

## 三、AI 智能导航：流程导航层

### 3.1 判断逻辑

用户说出问题后，AI 通过一次 LLM 调用判断问题类型：

```
判断维度：
1. 影响时间跨度：短期（<1月）/ 中期（1-12月）/ 长期（>1年）
2. 可逆性：容易撤销 / 有代价但可逆 / 不可逆
3. 价值冲突程度：单纯取舍 / 有矛盾但能调和 / 核心价值冲突
4. 用户情绪投入：随口一提 / 有些纠结 / 反复在想

评分规则：
- 4项中有2项及以上为"重"→ 深度追问
- 4项中有3项及以上为"轻"→ 闪电扫描
- 边界情况 → 默认深度追问（宁深勿浅）
```

### 3.2 导航表达

AI 判断后，用自然语言告知用户当前思维模式，让用户始终有方向感：

**深度模式导航语**：
> "这听起来是一个对你很重要的决定。我会用「深度追问」帮你把每个角度都想清楚——你可能要面对一些不太舒服的问题，但想清楚了，就不怕了。准备好了吗？"

**闪电模式导航语**：
> "日常小事，不用纠结太久。我帮你3分钟理清思路。"

**用户可手动切换**：
- 闪电模式底部显示："想更深入地想？切换深度追问 →"
- 深度模式无需降级（深度是默认值）

### 3.3 实现方式

在 `/api/chat` 中新增导航阶段的处理：

1. 对话首轮（`turnCount === 0`），先调用导航 Prompt 判断类型
2. 返回 `mode: 'deep' | 'lightning'`
3. 前端根据 mode 加载对应的阶段配置（`PHASES_DEEP` / `PHASES_LIGHTNING`）
4. 后续对话使用对应模式的 System Prompt

---

## 四、结构化引导：对话内嵌方案

### 4.1 设计原则

**工具不裸露，结构化不额外——它就在追问本身里。**

用户不需要"填写一个5W2H表格"，AI 会在追问中自然地引导用户结构化思考：

```
❌ 卡片方案（V1构思）：
  弹出 5W2H 卡片 → 用户填写 What/Why/Who/When/Where/How → 提交

✅ 对话内嵌方案（V2）：
  AI 追问："你说想换工作——
           是因为工作内容本身（What），
           还是因为工作环境里的人（Who），
           还是因为你觉得这份工作看不到未来（Why）？"
  → 用户自然回答，AI 获得了结构化信息
```

### 4.2 输入框预填充

在特定追问场景下，AI 的追问会附带一个**输入框预填充提示**，降低用户的回答门槛：

| 场景 | AI 追问 | 输入框预填充 |
|------|---------|------------|
| 立场澄清 | "你说想换工作是因为'无聊'——能具体说说吗？" | `我想换工作，主要是因为…` |
| Pre-Mortem | "假设3年后你换了工作，结果是一场灾难。最可能的原因是什么？" | `如果失败了，最可能是因为我…` |
| 盲区探测 | "如果过去的投入都不算，你还会做同样的选择吗？" | `如果从零开始，我会…` |
| 闪电-两面翻转 | "选B的话，最让你期待的是什么？" | `选B让我期待的是…` |

**实现方式**：
- 后端返回 `inputHint` 字段
- 前端在收到 AI 回复后，将 `inputHint` 填入 `<textarea>` 的 placeholder
- 用户点击输入框时，预填充文字保留（而非清空），用户可以直接续写

### 4.3 深度追问中的结构化追问策略

在深度模式的各个阶段，AI 的追问自带结构化方向，但表达为自然对话：

| 阶段 | 结构化工具（内部概念） | 对话内嵌表现 |
|------|---------------------|------------|
| 定义决策 | 5W2H 式拆解 | "你说想辞职——是现在的工作本身让你不满意（What），还是身边的人让你想离开（Who），还是你觉得到了该变的时候了（When/Why）？" |
| 立场澄清 | 价值 vs 代价的二分 | "你说'发展空间'——这个发展空间具体是什么画面？为了这个画面，你愿意放弃什么？" |
| Pre-Mortem | 二阶思维引导 | "你说'大不了再找工作'——但如果你创业两年后失败，简历空白，行业变了，那时候再找工作和现在一样吗？如果更难，你会怎么应对？" |
| 盲区探测 | 镜像问题 + 反直觉 | "你说'稳定很重要'——但如果稳定在困住你呢？如果突然失去这份工作，3年后的你会比现在更好还是更差？" |
| 未来回望 | 遗憾最小化 | "10年后的你，回看今天——她会希望你因为恐惧而不做，还是因为冲动而做了？" |

**关键**：这些结构化工具只是 AI 内部的策略指引，用户感知到的是"这个追问好精准"，而不是"我在填一个表格"。

---

## 五、技术方案：基于现有系统改造

### 5.1 改造清单

| 改造项 | 类型 | 说明 |
|--------|------|------|
| 新增导航阶段 | 后端 | 第一轮对话增加模式判断，返回 `mode` |
| 新增闪电模式阶段配置 | 后端 | `PHASES_LIGHTNING` 3 步快扫配置 |
| 新增闪电模式 System Prompt | 后端 | 轻快语气，3-5 轮完成 |
| `inputHint` 字段 | 后端+前端 | AI 回复附带输入提示，前端渲染到 placeholder |
| 阶段切换提示增强 | 前端 | 阶段推进时显示"▸ 进入XX阶段"（已有，保持） |
| 模式标识 | 前端 | 对话页顶部显示当前模式标签 |
| 闪电模式切换按钮 | 前端 | 闪电模式底部"切换深度追问"入口 |
| 闪电模式简短摘要 | 前端+后端 | 不走完整快照流程，生成简化版思考摘要 |
| 导航判断 Prompt | 后端 | 单独的 LLM 调用，判断问题类型 |

### 5.2 不改的部分

| 已有模块 | 处理方式 |
|---------|---------|
| 对话引擎核心（`buildSystemPrompt`） | 复用，新增模式参数 |
| 质量守门（`responseQualityCheck`） | 完全复用 |
| 约束重试机制 | 完全复用 |
| 上下文富化层（`buildConversationSummary`） | 完全复用 |
| 动态策略注入（`buildDynamicStrategy`） | 复用，扩展闪电模式策略 |
| 认知偏差识别 | 复用，闪电模式也启用（简略版） |
| 决策快照 | 深度模式复用，闪电模式生成简化版 |
| 决策日志 | 复用 |
| 用户体系 + 埋点 | 复用 |
| UI 整体框架 | 复用，微调 |

### 5.3 后端架构变化

```
用户输入
    │
    ▼
┌──────────────────┐
│  导航判断层（新增）│  ← 首轮调用，判断 mode
│  deep / lightning │
└────────┬─────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌──────────┐
│深度模式│ │闪电模式   │
│5 阶段  │ │3 步快扫   │
│完整快照│ │简短摘要   │
└───────┘ └──────────┘
    │         │
    ▼         ▼
  复用现有对话引擎（buildSystemPrompt + 质量守门 + 约束重试）
```

### 5.4 数据模型变化

在 Conversation 表中新增字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `mode` | TEXT | `'deep'` / `'lightning'`，默认 `'deep'` |
| `inputHint` | TEXT | AI 返回的输入框预填充提示（每轮） |

Message 结构新增：

| 字段 | 类型 | 说明 |
|------|------|------|
| `inputHint` | TEXT | 仅 assistant 消息，输入框预填充提示 |

---

## 六、闪电模式详细设计

### 6.1 阶段配置

```javascript
const PHASES_LIGHTNING = [
  {
    id: 'quick_define',
    label: '快速定性',
    minTurns: 1,
    maxTurns: 2,
    // AI 追问锁定决策核心维度
  },
  {
    id: 'quick_flip',
    label: '两面翻转',
    minTurns: 1,
    maxTurns: 2,
    // 快速翻面：价值 vs 代价
  },
  {
    id: 'quick_call',
    label: '一锤定音',
    minTurns: 1,
    maxTurns: 2,
    // 确认直觉 + 轻量后悔测试
  },
];
```

### 6.2 闪电模式 System Prompt 核心策略

```
你是"棱镜"的闪电模式——一个聪明、直接、不废话的朋友。

## 身份
你不是顾问，你是一个反应快、直觉准、敢说真话的朋友。
你的风格：直接、轻快、偶尔毒舌，但不刻薄。

## 核心约束
1. 永远不给建议——但可以用"如果是我，我会想…"的方式引导
2. 每轮只问一个核心问题，不铺陈
3. 追问必须引用用户的具体用词
4. 控制在3-5轮内完成
5. 语气轻快，不要沉重

## 阶段策略
- quick_define：一个追问锁定核心维度
- quick_flip：翻面——帮用户看到他没看的那一面
- quick_call：确认直觉，轻量后悔测试

## 禁止
- 不要说"这是一个重要的决定"——闪电模式就是小事
- 不要超过3句话
- 不要用苏格拉底式的长追问
```

### 6.3 闪电模式输出：简短思考摘要

对话结束后，生成简化版摘要（不需要完整快照）：

```json
{
  "title": "决策标题",
  "core_dimension": "这个决定对你来说最重要的是XX",
  "flip_insight": "你可能忽略了XX",
  "gut_feeling": "你的直觉倾向是XX",
  "regret_test": "3个月后如果不选XX，你会后悔吗？"
}
```

---

## 七、深度模式增强设计

### 7.1 与 V1 的差异

深度模式的核心流程（5 阶段）与 V1 一致，增强点在于：

1. **阶段进入时的模式告知**：首次进入深度模式时，AI 明确说"我会用深度追问帮你"
2. **结构化追问**：AI 追问自带结构化方向（见第四节）
3. **输入框预填充**：降低回答门槛
4. **阶段推进时的过渡语**：AI 在推进阶段时给出过渡语，而非冷切换

### 7.2 阶段过渡语设计

当 AI 判断当前阶段完成、需要推进时，在追问中自然过渡：

```
define → stance：
"我大概理解你的纠结了。接下来我想逐个看看——
 推动你改变的力量是什么？拦着你的又是什么？"

stance → premortem：
"你已经说了支持和反对的理由。
 但如果两个选择都走向最坏——你准备好了吗？"

premortem → blindspot：
"最坏的情况你想到了。
 但有没有一种可能——你一直在回避一个问题？"

blindspot → future：
"被击中的感觉不太好受。
 但我要问你最后一个问题——
 这次，从很远的地方看。"

future → 快照：
"好了。你想清楚了一些。
 让我把你今天说的都记下来。"
```

---

## 八、前端交互变化

### 8.1 首页

保持现有首页设计，不变。

### 8.2 对话页

**新增**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 模式标签 | 顶部 header 右侧 | 显示"深度追问"或"闪电扫描" |
| 输入框预填充 | 输入框 placeholder | AI 返回的 `inputHint` |
| 切换按钮（闪电模式） | 输入框上方 | "想更深入？切换深度追问 →" |

**保持不变**：
- 对话气泡样式
- 阶段进度指示
- 阶段推进提示条
- 发送按钮

### 8.3 快照页

- 深度模式：复用现有快照页
- 闪电模式：简化版摘要卡片（无信心指数、无 Pre-Mortem 分栏、无校准提醒）

---

## 九、MVP 范围

### 9.1 V2 MVP 包含 ✅

| 功能 | 优先级 | 说明 |
|------|:------:|------|
| AI 智能导航 | P0 | 判断问题类型，自动进入深度/闪电模式 |
| 深度追问模式 | P0 | 复用现有5阶段引擎 + 结构化追问增强 |
| 闪电扫描模式 | P0 | 新增3步快扫流程 |
| 对话内结构化引导 | P0 | AI 追问自带结构化方向，不造新交互 |
| 输入框预填充 | P1 | AI 回复附带 `inputHint`，降低回答门槛 |
| 模式标识 | P1 | 对话页显示当前模式 |
| 闪电→深度切换 | P1 | 闪电模式底部切换入口 |
| 决策快照（深度） | P0 | 复用现有 |
| 思考摘要（闪电） | P0 | 简化版输出 |
| 决策日志 | P0 | 复用现有 |
| 阶段过渡语 | P1 | 阶段推进时的自然过渡 |

### 9.2 V2 MVP 不包含 ❌

| 功能 | 原因 | 计划 |
|------|------|------|
| 设计思维流程 | 缩小MVP范围 | V2.1 |
| 系统思维流程 | 缩小MVP范围 | V2.1 |
| 工具卡片系统 | 用对话内嵌方案替代 | 不做 |
| 可视化引擎（因果回路图、雷达图） | 技术复杂度高 | V3.0 |
| 多专家Agent并行调度 | 架构改动大 | V3.0 |
| 校准回顾对话 | V1已有设计但未实现，V2延后 | V2.1 |
| 推送提醒 | 需要移动端基础设施 | V2.1 |

---

## 十、关键指标

### 10.1 北极星指标

**"深度完成率"** = 完成完整流程的用户 / 开始对话的用户

> V2 新增关注：闪电模式完成率 vs 深度模式完成率的对比，验证"给小决策轻量路径"是否能提升整体完成率。

### 10.2 V2 新增指标

| 指标 | 定义 | 目标 |
|------|------|------|
| 导航准确率 | AI 判断模式与用户预期一致的比例 | ≥ 85% |
| 闪电模式占比 | 闪电模式对话 / 全部对话 | 20-40% |
| 闪电→深度切换率 | 闪电模式中切换到深度的比例 | ≤ 30% |
| 闪电模式完成率 | 3步快扫完成比例 | ≥ 80% |
| 深度模式完成率 | 5阶段完成比例（V1基准60%） | ≥ 65% |

---

## 十一、开放问题

| # | 问题 | 影响 | 验证方式 |
|---|------|------|---------|
| 1 | AI 导航判断是否足够准确？用户会不会被错误分到闪电模式？ | 核心体验 | 内测10人，观察导航判断是否与用户预期一致 |
| 2 | 输入框预填充是否会让用户"偷懒"直接提交预填充内容？ | 数据质量 | 埋点追踪预填充内容的修改率 |
| 3 | 闪电模式3步是否足够？有些"小事"可能也需要更多轮 | 流程设计 | 观察闪电模式平均轮数，是否经常超过5轮 |
| 4 | 结构化追问（如5W2H式拆解）是否会让对话变得公式化？ | 对话质量 | 与V1对话质量评分对比 |
| 5 | 闪电模式是否需要校准提醒？ | 产品设计 | 先不加，观察用户是否主动回看闪电模式的决策 |

---

## 十二、里程碑

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| **V2-α** | 1周 | 导航判断 + 闪电模式3步流程跑通 |
| **V2-β** | 2周 | 深度模式结构化追问增强 + inputHint + 模式标识 + 闪电摘要 |
| **V2-RC** | 3周 | Bug修复 + 10人内测 + 导航准确率调优 |
| **V2上线** | 4周 | 开放测试 |

---

*PRD v2.0 完*

> **设计哲学提醒**：镜是思维的外骨骼。它不替你思考，但它让你无法偷懒地思考。  
> 每一个功能的设计，都要问自己：**这是在帮用户思考，还是在替用户思考？**  
> 如果是后者，砍掉。
