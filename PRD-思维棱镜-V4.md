# 镜·MindLens V4 产品需求文档

> 版本: v4.0
> 日期: 2026 年 5 月 8 日
> 状态: Draft
> 起点: V3 实际形态 = 5 阶段伙伴式对话 + 带注释小作文
> 一句话核心:**从"按阶段追问"变成"理解驱动的智者对话"——LLM 每轮先理解用户，再基于理解生成追问，不再受限于固定阶段**

---

## 〇、为什么有 V4

### 0.1 V3 的真实状态

V3 解决了"对话冷"和"报告工具感"两个问题：
- AI 从"审讯者"变成"严肃的思考伙伴"
- 快照从结构化字段变成带注释的小作文

**但 V3 保留了 5 阶段骨架**（define → stance → premortem → blindspot → future）。这是 V1 就有的设计，V2/V3 只改了语气和报告形态，没有动骨架。

### 0.2 V4 的三个触发点

**触发点 1:流程固定，用户能预判下一个问题**
用户第二次使用时："即使问题不同，但因为流程固定，我也能猜到 AI 下一个问题是什么。"——模板感来自骨架，不是语气。

**触发点 2:AI 的问题和引用用户的内容有割裂感**
用户反馈："像是套模板一样"——LLM 按阶段角度生成问题，问题本身是对的，但和用户的具体内容之间缺少"理解"的桥梁。

**触发点 3:覆盖人群窄**
大大咧咧的人说"没什么好纠结的"，前几轮就退出。5 阶段深度追问对轻量决策和直觉型用户是过度设计。

### 0.3 V3 PRD 里 V4 方向的修正

V3 PRD 预测 V4 主线是"思维成长伙伴"（跨对话洞察、成长档案）。**这个方向延后**。V4 先解决更根本的问题：**对话引擎本身**。

没有好的对话引擎，跨对话洞察只是重复 V3 的模板感。V4 聚焦单对话体验的重构。

---

## 一、产品定位

### 1.1 一句话定义

> **镜不替你思考，它听你说，然后问出你还没想到的问题——每一轮都是基于它对你的理解，不是按清单走。**

### 1.2 核心信念（不变）

- 人不缺导师，缺的是被好问题反复锤炼的机会
- 思考成长发生在"看见自己刚才是怎么想的"的瞬间
- AI 永远不替用户做决定

### 1.3 与 V3 的关键区别

| 维度 | V3 | V4 |
|------|-----|-----|
| 对话骨架 | 5 阶段（define/stance/premortem/blindspot/future） | **无阶段**——每轮基于理解动态生成 |
| LLM 角色 | 伙伴式阶段执行者 | **理解驱动的思考伙伴**——先理解，再追问 |
| 追问来源 | 阶段角度 + 动态策略提示 | **用户内容的理解**——追问扎根于对用户的洞察 |
| 完成逻辑 | 走完 5 阶段 + 轮数条件 | **LLM 感知清晰度后温和收束**——用户可继续 |
| 人群覆盖 | 深度决策用户 | **所有有"想聊聊"需求的人**——大事小事 |
| 用户体验 | "AI 在陪我走流程" | **"AI 在听我说话"** |

### 1.4 首页文案调整

```
镜

今天想聊点什么？

[随便说一件……]
[开始对话]
```

- 去掉"帮你想完整"——不预设用户需要帮助
- "纠结" → "聊"——覆盖所有场景
- "开始" → "开始对话"——强调对话而非流程

---

## 二、核心架构：理解-追问引擎

### 2.1 三层架构

```
┌────────────────────────────────────────┐
│  Layer 1: 理解引擎（Understanding）      │
│  ── 每轮分析用户回答，更新对话理解        │
│     → 输出: understanding 对象          │
├────────────────────────────────────────┤
│  Layer 2: 追问生成器（Response）         │
│  ── 基于 understanding 自由生成追问      │
│     → 输出: 追问内容 + 收束判断          │
├────────────────────────────────────────┤
│  Layer 3: 质量守门（Quality Gate）       │
│  ── 检测重复、跑题、建议、空洞肯定       │
│     → 输出: 通过 / 重试                  │
└────────────────────────────────────────┘
```

关键原则：**理解是主导**——追问完全基于理解生成，没有阶段约束、没有策略模板。

### 2.2 单次 LLM 调用输出格式

V4 每轮一次 LLM 调用，输出结构化 JSON：

```json
{
  "understanding": {
    "surface": "用户想辞职做设计师，怕养不活",
    "tension": "推力=画了五年的热爱，拉力=经济不安全感+父母看法",
    "key_moments": ["'真的舍不得'说得很慢", "'其实我不是怕没钱'是转折点"],
    "unsaid": "还没说到底怕父母看不起到什么程度"
  },
  "response": {
    "content": "追问内容（纯文本）",
    "is_winding_down": false,
    "wind_down_hint": null
  }
}
```

字段说明：

| 字段 | 类型 | 说明 |
|------|------|------|
| `understanding.surface` | string | 用户表面在说什么——用用户的原话概括 |
| `understanding.tension` | string | 核心张力是什么——推力 vs 拉力、渴望 vs 恐惧 |
| `understanding.key_moments` | string[] | 对话中的关键时刻——用户停顿、转折、重话 |
| `understanding.unsaid` | string | 用户还没说出的——基于已说内容的合理推断 |
| `response.content` | string | 本轮追问——基于 understanding 自由生成 |
| `response.is_winding_down` | boolean | 是否开始收束——LLM 感知到清晰度时设为 true |
| `response.wind_down_hint` | string \| null | 收束提示——如"想继续聊还是整理一下？" |

### 2.3 understanding 的积累性

understanding 不是每轮从零生成，而是**增量更新**：

1. 第一轮：基于用户输入生成初始 understanding
2. 第二轮及以后：基于上一轮 understanding + 新回答，更新理解
3. 更新规则：保留已确认的理解，修正变化的部分，添加新发现

示例积累过程：

```
第 1 轮:
  surface: "用户想辞职做设计师"
  tension: "推力=热爱设计，拉力=未知"
  unsaid: "还没说到底怕什么"

第 3 轮:
  surface: "用户想辞职做设计师，怕养不活"
  tension: "推力=画了五年的热爱+舍不得，拉力=经济不安全感+父母看法"
  key_moments: ["'真的舍不得'说得很慢"]
  unsaid: "还没说到底怕父母看不起到什么程度"

第 5 轮:
  surface: "用户想辞职做设计师"
  tension: "推力=热爱设计，拉力=怕被父母看不起（经济只是表面）"
  key_moments: ["'真的舍不得'说得很慢", "'其实我不是怕没钱'是转折点"]
  unsaid: "还没说现在工作里具体舍不得什么"
```

### 2.4 understanding 的前端显示

understanding 的内容优雅地显示在对话界面，让用户感知到"AI 在理解我"。

**显示方式**：
- 在对话界面的顶部或角落，用很轻量的文字显示当前核心张力
- 不在用户回答后立即变化，而是在 AI "思考"（打字机前的停顿）期间微妙更新
- 文字风格：陈述句，不带标签，像一个人的内心独白

示例显示：
```
镜在听：热爱设计 vs 怕父母看不起
```

**设计原则**：
- 不侵入：不弹窗、不高亮、不打断对话流
- 不粗糙：不用标签、不用进度条、不用图标
- 可关闭：用户可以在设置中关闭这个显示

---

## 三、Prompt 体系

### 3.1 System Prompt 结构

V4 的 system prompt 分为三部分：

```
【角色定义】
你是镜——一个严肃的思考伙伴。
你不是分析师，不是导师，不是治疗师。
你是那种"对方说话时你真的在听"的人——你会接住，会反映，
会基于你听到的内容，问出让对方继续往下想的问题。

你的工作不是"问出答案"，是"陪 ta 把没想清楚的都说出来"。

【当前对话理解】
这是你们已经聊过的内容：
- 用户表面在说的是：<surface>
- 核心张力是：<tension>
- 关键时刻：<key_moments>
- 还没说出的：<unsaid>

【输出格式要求】
请以 JSON 格式输出，包含 understanding 和 response 两个字段。
understanding 是你对当前对话的更新理解。
response.content 是你下一轮想问的话——基于你的理解自由生成，
不要按任何固定套路，只要这个问题能帮你更理解用户。

【铁律】
1. 不给建议，不下结论——决策永远是 ta 的事
2. 不空洞肯定——不说"你说得对""我完全理解"
3. 必须有引导——每轮回复要让用户有话可说（疑问/邀请/留白）
4. 不与之前重复——本轮追问的核心角度与最近 2 轮不重复
5. 理解与追问一致——如果 understanding 标注了 unsaid，
   追问应该针对 unsaid，而不是无关方向
```

### 3.2 追问生成的自由度

LLM 的追问生成**完全自由**，不受以下约束：
- ❌ 没有阶段角度（没有"define 阶段你该问什么"）
- ❌ 没有策略模板（没有"本轮用翻转型/场景型"）
- ❌ 没有轮数要求（没有"这个阶段至少 3 轮"）

唯一约束是**理解本身**——追问必须基于 understanding 生成。

这意味着同一个用户、同一个决策，两次对话的追问路径完全不同——因为 LLM 的理解和选择会不同。

### 3.3 收束机制

当 LLM 感知到用户已有足够清晰度时，`is_winding_down` 设为 true：

```json
{
  "response": {
    "content": "听起来你已经比较清楚了。想继续往下走走，还是整理一下今天聊的？",
    "is_winding_down": true,
    "wind_down_hint": "想继续聊还是整理一下？"
  }
}
```

**前端处理**：
- 追问正常显示
- 在追问后显示两个选项按钮："继续聊" / "整理一下"
- 用户选择"继续聊" → 对话继续，LLM 继续生成追问
- 用户选择"整理一下" → 触发快照生成
- 不设硬轮数上限，但建议 LLM 在 15 轮后主动提示收束

---

## 四、质量守门 V4

### 4.1 保留规则

| 规则 | 说明 |
|------|------|
| 不给建议 | 检测"你应该/我建议/最好/推荐"等 |
| 不空洞肯定 | 检测"你说得对""我完全理解" |
| 必须有引导 | 检测疑问/邀请/留白——让用户有话可说 |

### 4.2 删除规则

| 规则 | V3 状态 | V4 处理 |
|------|---------|---------|
| 模板句式检测 | 保留（3 条） | **删除**——自由生成场景下意义不大 |

### 4.3 新增规则

| 规则 | 说明 |
|------|------|
| 不与之前重复 | 本轮追问的核心角度与最近 2 轮不重复 |
| 理解与追问一致 | understanding 标注了 unsaid，追问应针对 unsaid |

### 4.4 重试触发

仅当违反铁律（不给建议）或同时违反"不重复 + 无引导"时触发重试。

---

## 五、快照系统（保留 V3 味道）

### 5.1 小作文结构（不变）

V3 的小作文形态完全保留：
- 以用户原话为骨架
- AI 用最少连接词串联
- 按对话时间顺序组织
- 关键转折必须留下来
- 长度 200-350 字

### 5.2 去掉阶段痕迹

V3 小作文中隐含的 5 阶段结构不再体现。叙事完全按对话自然流：

```
你今天来，带着这件事：
[[1]]我想辞职去做设计师，但怕养不活自己。

聊到深处时，你说：
[[2]]我已经画了五年了，真的舍不得。

然后停了很久，才说出：
[[3]]其实我不是怕没钱，是怕被父母看不起。

那一刻，所有的纠结终于有了答案。
```

**禁止出现**："define 阶段""stance 阶段""进一步追问时"等阶段术语。

### 5.3 注释系统（不变）

V3 的 3 类注释（reflection / observation / gentle_prompt）和铁律完全保留。

### 5.4 后端校验（不变）

anchor 必须在 essay 和用户消息中都能 substring 匹配，否则丢弃。

---

## 六、前端改动

### 6.1 首页文案

```html
<div class="logo">镜</div>
<!-- 删除: <div class="logo-sub">帮你想完整</div> -->
<h2 class="ask">今天想聊点什么？</h2>
<textarea placeholder="随便说一件……"></textarea>
<button>开始对话</button>
```

### 6.2 阶段角标移除

删除对话界面的阶段角标（"定义 1/5"），替换为 understanding 的优雅显示。

### 6.3 understanding 显示

在 AI 思考期间（打字机前），在界面的某个轻量位置显示当前理解：

```
镜在听：热爱设计 vs 怕父母看不起
```

**技术要求**：
- 用 CSS transition 做淡入淡出，不突兀
- 文字颜色用浅灰色，不抢对话主体
- 可关闭（设置项）

### 6.4 收束交互

当 `is_winding_down` 为 true 时：
- 追问后显示两个轻量按钮："继续聊" / "整理一下"
- 按钮样式与对话风格一致，不突兀

### 6.5 历史回看（不变）

历史回看 overlay 保留，显示对话时间线。

---

## 七、文件改动清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `routes/chat.js` | **重写** | 移除 phase/turnCount，接入 understanding 引擎 |
| `prompts/deep-prompt.js` | **重写** | 新 system prompt：理解驱动 + JSON 输出格式 |
| `prompts/phases.js` | **删除** | 5 阶段配置不再使用 |
| `prompts/quality-gate.js` | 修改 | 删除模板句式检测，新增不重复、理解一致检测 |
| `public/app.js` | 修改 | 移除阶段角标，加 understanding 显示，加收束交互 |
| `public/index.html` | 修改 | 首页文案调整 |
| `public/styles.css` | 修改 | understanding 显示样式，收束按钮样式 |
| `routes/chat.js` 中 `/api/snapshot` | 微调 | snapshot prompt 去掉阶段痕迹 |

---

## 八、衡量 V4 是否成功

### 8.1 主指标（主观，PM 自评）

V4 上线后 PM 自己用 5 次对话：

- **≥ 4 次** 对话过程中感觉"AI 真的在听我"（理解感）
- **≥ 3 次** 第二次使用时**没有**"这个问题之前问过"的感觉（新鲜感）
- **≥ 3 次** 对话结束时感觉"这是自然结束的，不是走完流程"（收束感）

### 8.2 辅指标（客观）

| 指标 | V3 当前 | V4 目标 |
|------|---------|---------|
| 对话轮数分布 | 集中在 12-18 轮 | 分布更宽（5-20 轮），反映自适应深度 |
| 收束后继续率 | 无（V3 强制结束）| ≥ 30% 用户选择"继续聊" |
| 首页退出率 | 高（纠结型文案筛人）| 降低（开放文案降低门槛）|

### 8.3 失败信号

| 信号 | 含义 |
|-----|------|
| LLM 开始重复提问 | understanding 机制失效，加不重复规则 |
| 追问与 understanding 脱节 | 理解-追问一致性规则没生效 |
| 对话过长（> 20 轮）且无收束 | LLM 收束感知失效，加轮数软上限 |
| 快照中出现阶段术语 | snapshot prompt 没改干净 |

---

## 九、不做的事

| 事项 | 处理 |
|------|------|
| 恢复双模式/闪电扫描 | **永久不做**——V4 的自适应深度已覆盖 |
| 5 阶段任何形式的回归 | **永久不做**——阶段是模板化的根源 |
| 显式策略库（翻转型/场景型等） | **永久不做**——智者不问套路 |
| 思维成长档案/跨对话洞察 | **延后到 V5**——V4 聚焦单对话引擎 |
| 多 Agent / 工具卡片 / 可视化 | **永久不做**（V3 已划掉）|

---

## 十、技术实现要点

### 10.1 LLM 输出解析

LLM 输出 JSON，后端解析：
- `understanding` → 存入 conversation 记录，下轮传回
- `response.content` → 返回前端显示
- `response.is_winding_down` → 前端控制收束 UI

### 10.2 understanding 存储

```javascript
// conversation 记录新增字段
{
  id: "...",
  messages: [...],
  understanding: {        // 新增
    surface: "...",
    tension: "...",
    key_moments: [...],
    unsaid: "..."
  },
  round_count: 0,         // 替代 phaseTurnCount
  is_winding_down: false  // 新增
}
```

### 10.3 质量守门时机

在 LLM 输出解析后、返回前端前执行：
1. 解析 JSON
2. 对 `response.content` 执行质量守门
3. 如触发重试，用约束 prompt 重新调用 LLM
4. 返回最终结果

### 10.4 温度参数

V4 不设阶段温度，统一用 `temperature: 0.7`——理解驱动需要一定的创造性，不宜过低。

---

## 十一、设计哲学

> **V4 改的不是语气，是"提问的来源"。**
>
> V1-V3 的追问来自"阶段角度"——LLM 知道自己在哪个阶段，按角度生成问题。
> V4 的追问来自"理解"——LLM 先形成对用户的理解，然后基于这个理解生成问题。
>
> 阶段是"从外向内"（先定框架，再往里面填）。
> 理解是"从内向外"（先听内容，再决定往哪走）。
>
> 每加一个新功能时问自己：**这是让 LLM 更"懂"用户，还是给了 LLM 更多"套路"？**
> 如果是后者，砍掉。

---

*PRD V4.0 完*
